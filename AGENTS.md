# cni_blocks 作業ルール

## 基本方針

- このフォルダだけを独立したWordPressプラグインとして扱います。
- `PROJECT-BRIEF.md` が存在する場合は、作業前に最後まで読み、ブロックの優先順位と初期仕様を確認します。
- 最初に既存構成とプラグインヘッダーを確認し、依頼されていない機能変更は行いません。
- 既存のスライドギャラリーとタイルギャラリーのブロック名、属性、保存済みコンテンツとの互換性を維持します。
- 新しいブロック名には `cni-blocks/` 名前空間を使用し、他プラグインのブロック名と衝突させません。
- VK Blocks Proやその他の有料製品のコード、非公開素材、保存マークアップをコピーしません。公開情報と実際の用途を参考に、WordPress標準APIで独立実装します。
- WordPress標準ブロック、ブロックスタイル、ブロックバリエーションで安全に実現できる場合は、新規ブロックを増やさない方法を優先します。
- コンテナ系ブロックは内部に標準ブロックを配置できる構造を優先し、利用者のコンテンツを独自形式へ閉じ込めないようにします。
- 大きな一括変更を避け、目的ごとに小さく変更します。
- 現在のファイル構成を大きく変更する場合は、先に理由、移行方法、既存ブロックへの影響を報告し、機能追加と同時に無断で全面改修しません。
- WordPress標準APIを優先し、入力のサニタイズ、出力のエスケープ、必要な権限・nonce確認を行います。
- エディター表示と公開画面の表示が一致すること、キーボード操作と見出し構造などのアクセシビリティを確認します。
- 変更後は、変更ファイル、確認結果、WordPress上の手動確認手順を説明します。
- プロジェクト内の既存ZIPは参照用バックアップとして扱い、明示的な依頼なしに上書きしません。

## 通常の開発

通常の機能追加、不具合修正、コード整理では、ローカルファイルの変更と必要な構文検査まで行います。

ユーザーから明示的な指示がない限り、次は行いません。

- `git add`
- `git commit`
- `git push`
- Pull Request作成
- `main` へのマージ
- Version変更
- 配布ZIP生成

## バックアップ

ユーザーが「バックアップして」と指示した場合は、次を行います。

1. 現在の変更内容と `git status`、差分を確認します。
2. PHP、JavaScript、JSONなど、対象に応じた構文検査を行います。
3. 機密情報、不要ファイル、ZIP、バックアップファイルがGit管理対象に含まれていないことを確認します。
4. 必要なファイルだけを `git add` します。
5. 内容に合ったコミットメッセージで `git commit` します。
6. 現在の作業ブランチをGitHubへ `git push` します。
7. push後のブランチ、コミットID、GitHubとの同期状態を報告します。

バックアップは通常のGitコマンドによる `git add`、`git commit`、`git push` までとし、GitHub CLI（`gh`）を必須としません。`gh` が導入されていない場合もバックアップを停止せず、push後の状態確認まで完了します。

バックアップでは次を行いません。

- Version変更
- 配布ZIP生成
- Pull Request作成
- `main` へのマージ

## リリース

ユーザーが「リリースして」と指示した場合は、次を行います。

1. 現在の変更内容と `git status`、差分を確認します。
2. PHP、JavaScript、JSON、PowerShellなど、対象に応じた構文検査を行います。
3. WordPress上で確認すべき項目を整理します。
4. 次のVersionを確認します。
   - ユーザーがVersionを指定した場合は、そのVersionを使用します。
   - Versionの指定がない場合は、変更内容に応じたVersion案を提示し、ユーザーの確認を得るまで変更しません。
5. プラグイン本体のVersionと `readme.txt` の `Stable tag` を一致させます。
6. `build-release.ps1` を実行します。ファイルが存在しない場合は処理を停止して報告します。
7. `release` フォルダへ配布ZIPを生成します。
8. ZIP内を次の観点で検証します。
   - 最上位フォルダ名が正しいこと
   - 必須ファイルが含まれていること
   - `.git`、`AGENTS.md`、`PROJECT-BRIEF.md`、`build-release.ps1` などの開発用ファイルが含まれていないこと
   - 二重フォルダ構造になっていないこと
9. 必要なファイルだけを `git add` します。
10. 内容に合ったコミットメッセージで `git commit` します。
11. 現在の作業ブランチをGitHubへ `git push` します。
12. Pull Requestを作成します。
13. リリース結果を報告します。

リリース結果には次を含めます。

- プラグイン名
- Version
- 実施した検査
- 生成ZIPの場所
- コミットID
- push先ブランチ
- Pull Request番号とURL
- `main` が未マージであること
- WordPressで確認すべき項目

リリースでは、ユーザーから明示的な許可がない限り `main` へマージしません。

## Git運用

- `main` は公開可能な安定版として扱います。
- 通常の変更は作業ブランチで行います。
- `main` へ直接commitしません。
- バックアップまたはリリースの指示時に現在のブランチが `main` の場合は、作業ブランチを作成してからcommitします。
- Pull Requestを経由して `main` へ統合します。
- pushやPull Request作成前に `git status` と差分を確認します。
- ユーザーの明示的な指示なしにブランチ削除を行いません。

## GitHub CLIとPull Request

- `gh` の導入確認が必要なのは、Codex自身がPull Requestを作成する場合だけです。
- バックアップではPull Requestを作成しないため、`gh` の有無を確認せず、通常のGitコマンドでpushまで実施します。
- `gh` がない場合でも、commitとpushまでは完了し、Pull Requestを作成できなかった場合はその工程だけを未実施として報告します。
- ChatGPTのGitHub連携やGitHub画面からPull Requestを作成できるため、`gh` 未導入をリリース全体の失敗とは扱いません。

## 配布物

- `release` フォルダとZIPはGit管理しません。
- 配布ZIPは `build-release.ps1` で生成します。
- 既存ZIPは通常上書きしません。
- 上書きが必要な場合のみ `-Force` を使用します。
- 配布ZIPの構造検証に失敗した場合はリリース処理を停止し、不完全なZIPを残しません。
