# cni_blocks 拡張方針

## 目的

無料版Lightningを使用する実案件で不足しやすいブロック機能を、テーマから独立した追加プラグインとして提供します。

有料製品の完全な再現ではなく、実際に使用頻度が高く、保守可能な機能だけを独立して実装します。Lightning以外のテーマでも可能な限り利用できる設計を優先します。

## 既存機能

- スライドギャラリー `cni-blocks/slide-gallery`
- タイルギャラリー `cni-blocks/tile-gallery`

既存ブロックの名前、属性、保存済みコンテンツとの互換性を維持します。

## 優先順位

### 第1優先：Outer

中に自由なブロックを配置できるセクション用コンテナを作ります。

初期候補：

- 背景色
- 背景画像
- 背景画像の位置
- オーバーレイ色と透明度
- PC・モバイル別の背景画像
- 通常幅と全幅
- 内側コンテンツ幅
- PC・タブレット・モバイル別の内側余白
- 枠線と角丸
- HTML要素 `div` / `section` など
- InnerBlocksによる自由な内部ブロック配置

初期対象外：

- 波、カーブ、傾斜などの区切り形状
- 背景オフセット
- パララックス
- ブロック全体リンク
- 過剰に細かな端末別設定

### 第2優先：グリッドカラムカード

カラム数を直接指定するブロックではなく、カードの最小幅をpxで指定し、表示領域に入るカラム数を自動計算するレスポンシブグリッドを作ります。

必須候補：

- PC・タブレット・モバイル別のカード最小幅（px）
- 未入力の端末設定は上位設定を継承
- `auto-fit` / `minmax()` 相当による自動カラム計算
- 狭い画面で横スクロールを発生させず1カラムへ縮退
- カード間の横・縦の間隔
- カードの追加、削除、並べ替え
- カードの高さをそろえる
- ボタンなどをカード下端へそろえる
- 各カード内に標準ブロックを自由に配置
- エディターと公開画面で同じ折り返し動作

この自動グリッド処理は、後から投稿リストにも共通利用できる構造を検討します。

### 第3優先：コンテンツ一覧

投稿リストと選択投稿リストを一つの動的ブロックへまとめる方向で検討します。

取得方法：

- 条件に一致する記事を自動取得
- 記事を一件ずつ手動選択

初期候補：

- 投稿、固定ページ、公開されているカスタム投稿タイプ
- カテゴリーまたはカスタム分類による絞り込み
- 表示件数、並び順
- 現在の記事を除外
- カード表示と簡易一覧表示
- アイキャッチ、タイトル、抜粋、日付、分類の表示切り替え
- グリッドカラムカードと共通の自動カラム処理

### その後の候補

1. 横並びボタン
   - 左・中央・右揃え
   - ボタン間隔
   - スマホ時の縦並び
   - 標準ボタンまたは無料VKボタンを内部に配置
2. アコーディオン
   - WordPress標準の詳細ブロックを拡張する方法を優先
   - 独自JavaScriptを増やさず、スタイルと使い勝手を補う
3. ステップ・タイムライン
   - 親子ブロック
   - 各項目内に標準ブロックを配置可能
4. ブログカード
   - 最初は自サイト内の記事を対象
   - 外部URL取得はキャッシュ、取得失敗、安全対策を設計してから検討
5. パンくずリスト
   - Lightning標準やSEOプラグインとの重複があるため最後に判断
   - ブロックテーマ対応まで対象を広げる場合の候補

## 開発原則

- 有料製品のブロック名、コード、保存形式、デザインを複製しない。
- 一般的な用途を満たす独自のUIと保存形式を設計する。
- 静的なレイアウトブロックは可能な限りInnerBlocksを利用する。
- 投稿一覧などのデータ依存ブロックは、必要に応じてPHPで安全に動的描画する。
- 属性数を必要最小限にし、将来のブロック更新で保存済みページが壊れにくい構造にする。
- 共通のレスポンシブ幅、余白、色設定を再利用できるようにする。
- 一度に複数ブロックを実装せず、Outerから一つずつ設計、実装、確認する。

## 最初の作業

ファイルを変更する前に、現在のプラグイン構造を調査して以下を報告します。

1. 既存2ブロックの登録・保存・公開画面表示の流れ
2. 現在の構成へOuterを安全に追加できるか
3. ファイル分割やビルド環境が必要か
4. 既存ブロックを壊さず段階的に整理する方法
5. Outer初期版の最小ファイル構成と確認方法
